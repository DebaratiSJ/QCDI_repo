{
  "id": "products",
  "name": "Products",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_products",
      "name": "Products",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "lnd_onboarding_a-ushc",
      "schema": "{{task.lnd_onboarding_a-ushc:null$_$dbo.schema}}"
    }
  ]
}